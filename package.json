{
  "name": "homegate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.17.0" },
  "scripts": {
    "dev": "wrangler dev --config wrangler.homegate.jsonc",
    "deploy": "wrangler deploy --config wrangler.homegate.jsonc --keep-vars",
    "build": "npm run build:bundles && npm run verify:bundles",
    "build:bundles": "node scripts/build-bundles.mjs",
    "verify:bundles": "node scripts/verify-bundles.mjs",
    "deploy:installer": "wrangler deploy --config wrangler.installer.jsonc --keep-vars",
    "deploy:homegate": "wrangler deploy --config wrangler.homegate.jsonc --keep-vars",
    "typecheck": "tsc --noEmit && tsc --project tsconfig.tools.json --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "register:commands": "tsx --env-file=.env scripts/register-commands.ts"
  },
  "cloudflare": {
    "bindings": {
      "DISCORD_PUBLIC_KEY": {
        "description": "Discord Developer Portal > General InformationのApplication Public Key（64桁の16進数）。Workerの署名検証にだけ使います。秘密値はこのDeploy画面またはCloudflare DashboardのSecretとして入力してください。"
      },
      "DISCORD_CLIENT_ID": {
        "description": "Discord ApplicationのClient ID（Application ID）。Client Credentials Grantでコマンド登録に使います。"
      },
      "DISCORD_CLIENT_SECRET": {
        "description": "Discord OAuth2のClient Secret。Cloudflare Secretにだけ保存し、Workerのレスポンスやログへ出しません。"
      }
    }
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
